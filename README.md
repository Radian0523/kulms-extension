# KULMS Extension

京都大学LMS ([KULMS](https://lms.gakusei.kyoto-u.ac.jp/)) のUIをカスタマイズするChrome拡張機能です。

## 機能一覧

全9機能を搭載し、ヘッダーの「Ext」ボタンから設定画面を開いて個別にオン/オフできます。

### 1. 設定画面
ヘッダー右端の「⚙ Ext」ボタンからパネルを開き、歯車アイコンで設定画面に切り替え。
- 全9機能のオン/オフをトグルスイッチで管理
- 設定は `chrome.storage` に保存され、ページ再読み込み後に反映

### 2. テーマ切り替え
ヘッダーのテーマパネルから、4種類のテーマに切り替えられます。
- **デフォルト** — 標準の白背景
- **ダーク** — 暗い背景で目に優しい
- **セピア** — 温かみのある色合い
- **ブルー** — 青系の落ち着いたトーン

### 3. 全科目課題一覧パネル
Extパネルで全科目の課題を一覧表示します。
- Sakai Direct API (`/direct/assignment/`) から各コースの課題を取得
- 期限順にソート、期限が近い課題をハイライト表示
- パネル開閉時にページコンテンツを押し出して表示

### 4. 教科書パネル
KULASISシラバスから教科書・参考書情報を取得して表示します。
- Amazonへのリンク付き

### 5. 授業資料ツリービュー
授業資料ページで、フォルダ/ファイルの階層構造を視覚的に表示します。
- フォルダとファイルの深さに応じたインデントガイドライン
- フォルダの展開/折りたたみをページ遷移なしで実行

### 6. 科目名の整理 + ピン留めソート
- 科目名から年度・学期情報を除去して短縮表示（例: `[2026前期月1]パワーエレクトロニクス` → `[月1]パワーエレクトロニクス`）
- ピン留めした科目を曜日・時限順に自動ソート（月1 → 月2 → 火1 → ... → 金5）

### 7. コース行クリックで展開/折りたたみ
- 科目名のテキスト部分だけでなく、行全体をクリックでツールリストの展開/折りたたみが可能

### 8. ツール表示管理
各科目のツールリストを整理し、よく使うツールだけを表示します。
- **デフォルト表示**: 概要、授業資料（リソース）、課題
- それ以外のツールは「その他 ▶」トグルの下に折りたたみ
- 科目ごとにカスタマイズ可能（ホバーで表示される +/− ボタン）

### 9. サイドバーリサイズ
- サイドバーの右端をドラッグで幅を変更可能（120px〜500px）
- 幅は `localStorage` に保存され、次回アクセス時も維持

## インストール方法

1. このリポジトリをクローン
   ```bash
   git clone https://github.com/Radian0523/kulms-extension.git
   ```
2. Chrome で `chrome://extensions` を開く
3. 右上の「デベロッパーモード」を有効にする
4. 「パッケージ化されていない拡張機能を読み込む」をクリック
5. クローンしたフォルダを選択

## ファイル構成

```
kulms-extension/
├── manifest.json     # Chrome拡張マニフェスト (Manifest V3)
├── content.js        # メインのコンテンツスクリプト（全機能のIIFE）
├── styles.css        # テーマ・各機能のスタイル
├── background.js     # サービスワーカー（開発用）
└── hot-reload.sh     # ファイル変更監視スクリプト（開発用）
```

## 対応環境

- Google Chrome (Manifest V3)
- 対象サイト: `https://lms.gakusei.kyoto-u.ac.jp/*` (Sakai LMS)
